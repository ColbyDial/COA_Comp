using UnityEngine;
using System.Collections;

public class Running : MonoBehaviour {
	//perform a fetch every time, perform a set of control signals based on the op code from the fetch


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	public void run(){
		Debug.Log ("This button is now running!");
	}
}
